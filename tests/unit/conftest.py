import pytest
from typing import Dict


@pytest.fixture()
def noise_requirements() -> Dict:
    """From the docs, the following columns and respective noise types are
    required per form:
    """
    pass